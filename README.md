This Docker-compose will create images for frontend, backend and database and run container from images

In the docker-compose and dockerfile we have been used following options.

Environment Variables
Build Arguments
Docker Network
Docker Volumes

